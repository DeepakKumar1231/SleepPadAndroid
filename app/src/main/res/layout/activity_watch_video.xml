<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/white"
    tools:context=".Controller.WatchVideo">

<TextView
    android:id="@+id/welcomeTv"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="20dp"
    android:gravity="center"
    android:textSize="18sp"
    android:textStyle="bold"
    android:textColor="@color/purple"
    android:text="We welcome you to the SLEEP PAD Community ,where technology meets comfort."
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/logoIv"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:adjustViewBounds="true"
        android:padding="10dp"
        android:src="@drawable/indicator_image"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.356" />

    <Button
        android:id="@+id/preOrdernowBtn"
        android:layout_width="0dp"
        android:layout_height="45dp"
        android:text="Pre-Order Now"
        android:textSize="12sp"
        android:paddingHorizontal="40dp"
        android:layout_margin="10dp"
        android:background="@drawable/rectangle_bg"
        android:backgroundTint="@color/navy_blue"
        android:textColor="@color/white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/watchVideoBtn"
        app:layout_constraintTop_toBottomOf="@id/logoIv"
        app:layout_constraintBottom_toBottomOf="parent"/>

<Button
    android:id="@+id/watchVideoBtn"
    android:layout_width="0dp"
    android:layout_height="45dp"
    android:layout_margin="10dp"
    android:text="Watch Video"
    android:textSize="12sp"
    android:background="@drawable/rectangle_bg"
    android:backgroundTint="@color/navy_blue"
    android:textColor="@color/white"
    app:layout_constraintStart_toEndOf="@id/preOrdernowBtn"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toBottomOf="@id/logoIv"
    app:layout_constraintBottom_toBottomOf="parent"/>

<Button
    android:id="@+id/watchDemoBTn"
    android:layout_width="wrap_content"
    android:layout_height="45dp"
    android:layout_margin="10dp"
    android:text="Watch Demo"
    android:textSize="12sp"
    android:paddingHorizontal="40dp"
    android:background="@drawable/rectangle_bg"
    android:backgroundTint="@color/navy_blue"
    android:textColor="@color/white"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toBottomOf="@id/watchVideoBtn"
    app:layout_constraintBottom_toBottomOf="parent"/>

</android.support.constraint.ConstraintLayout>